  <section class="media-and-text image-right" id="sick-seats-section">
    <div class="container media-and-text-container">
      <div class="row">
        <div class="col-md-5 p-1">
      <div class="media-content">
        <img src="{{section2.image}}" alt="{{section2.title}}">
      </div>
      </div>
      <div class="col-md-6 offset-md-1 p-3">
      <div class="text-content">
        <h2><a href="{{metadata.url}}">{{section2.title}}</a></h2>
        <p class="subtitle">{{section2.intro}}</p>
        <div class="features-list">
        {% for se3 in section2.text %}
          <div class="feature-item">
            <div class="feature-icon"></div>
            <div class="feature-text">{{se3.info}}</div>
          </div>
          {% endfor %}
        </div>
        <div class="award">
          <img src="{{section2.awards_image}}" alt="{{section2.title}}">
          <p class="mt-3">{{section2.awards_text1}}</p><p class="mt-3">{{section2.awards_text2}}</p>
        </div>
        </div>
      </div>
    </div>
  </section>
